{
	"name": "cloudflare",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
    "cloudflared": "command -v ../bin/cloudflared >/dev/null 2>&1 && ../bin/cloudflared tunnel --url http://localhost:8787 || echo 'cloudflared not found. Download into ./bin/: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/'"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"typescript": "^5.5.2",
		"wrangler": "^4.84.1"
	},
	"dependencies": {
		"cbor-x": "^1.6.4"
	}
}